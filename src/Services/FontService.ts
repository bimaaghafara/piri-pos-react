import {scaleService} from '.';

export class FontService {
  xxs: number= scaleService.horizontal(8);
  xs: number= scaleService.horizontal(12);
  sm: number= scaleService.horizontal(16);
  md: number= scaleService.horizontal(20);
  lg: number= scaleService.horizontal(24);
  xl: number= scaleService.horizontal(28);
  xxl: number= scaleService.horizontal(32);
}